import { Request, Response } from "express";

const createCar = async (req: Request, res: Response)=>{
    try{
       const car = req.body;

       const result = await 
    }catch(error){
        console.log(error)
    }
}

