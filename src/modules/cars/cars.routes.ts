import express from "express";


const router = express.Router();

router.post('/', );
router.get('/', );
router.get('/', );
router.put('/:carId', );
router.delete('/:carId', );

export const CarRoutes = router;